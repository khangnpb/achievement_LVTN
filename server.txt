IP chính 	160.191.51.145
Username	Administrator hoặc root
Password	zKS}:aB0=2GG


IP chính 	103.9.77.227
Username	Administrator hoặc root
Password	6E5N78ws!dPuN

	

///// các bước cài docker /////

sudo apt install gnome-terminal
sudo apt-get update

# Add Docker's official GPG key:
sudo apt-get update
sudo apt-get install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

# Add the repository to Apt sources:
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt-get update

sudo apt-get install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

//////// cài odoo ///////////

 git clone https://github.com/khangnpb/achievement_LVTN.git odoo-crm
 git clone https://github.com/khangnpb/odoo.git odoo

/////// Build docker ///////////

docker build -t crm-odoo:16 .

////// cài certbot   ///////////////////

apt update
apt install -y certbot python3-certbot-nginx
certbot certonly --webroot -w /usr/share/nginx/html -d n8n.tuankiet.media --email dugkiet@gmail.com --agree-tos --no-eff-email
certbot --nginx -d n8n.tuankiet.media --email dugkiet@gmail.com --agree-tos --no-eff-email
certbot --nginx -d crm.tuankiet.media --email dugkiet@gmail.com --agree-tos --no-eff-email
certbot --nginx -d crm.duyan.vn --email dugkiet@gmail.com --agree-tos --no-eff-email	

/// cấp quyền

find . -type f -exec chmod 644 {} \;
find . -type d -exec chmod 755 {} \;

///update module
/opt/odoo/odoo/odoo-bin -c /opt/odoo/odoo.conf -d DAC01 -u modern_theme